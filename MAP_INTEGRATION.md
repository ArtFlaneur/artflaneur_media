# Интеграция кастомного стиля MapLibre

## Текущая реализация

Сейчас используется базовый OSM стиль. Чтобы использовать ваш кастомный стиль из приложения:

### Вариант 1: URL кастомного стиля

Если у вас есть JSON файл стиля, замените в `MapPage.tsx` (строка ~40):

```typescript
map.current = new maplibregl.Map({
    container: mapContainer.current,
    style: 'https://your-style-url.com/style.json', // <-- Ваш URL
    center: [0, 20],
    zoom: 2,
});
```

### Вариант 2: Inline стиль

Если стиль хранится локально, используйте объект стиля:

```typescript
map.current = new maplibregl.Map({
    container: mapContainer.current,
    style: {
        version: 8,
        sources: {
            // Ваши источники тайлов
        },
        layers: [
            // Ваши слои
        ]
    },
    center: [0, 20],
    zoom: 2,
});
```

### Вариант 3: MapTiler / Mapbox API

Если используете MapTiler или Mapbox:

**MapTiler:**
```typescript
style: `https://api.maptiler.com/maps/streets/style.json?key=${YOUR_API_KEY}`
```

**Mapbox (через MapLibre):**
```typescript
style: `https://api.mapbox.com/styles/v1/your-username/your-style-id?access_token=${YOUR_TOKEN}`
```

## Где взять стиль из мобильного приложения

1. **Найдите файл стиля** в вашем React Native проекте
2. Обычно это файл типа `mapStyle.json` или конфигурация MapLibre
3. Скопируйте URL или JSON объект

## Настройка переменных окружения

Если нужен API ключ, добавьте в `.env.local`:

```bash
VITE_MAP_STYLE_URL=https://your-map-style.com/style.json
VITE_MAPTILER_KEY=your-api-key
VITE_MAPBOX_TOKEN=your-token
```

Затем используйте в коде:

```typescript
style: import.meta.env.VITE_MAP_STYLE_URL
```

## Функции карты

✅ **Реализовано:**
- Zoom in/out (колесико мыши, кнопки +/-)
- Pan (перетаскивание)
- Маркеры галерей с Directus
- Клик по маркеру → карточка галереи
- Near Me (геолокация)
- Поиск галерей
- Фильтрация по типу

## Следующие шаги

Пришлите мне:
1. URL вашего кастомного стиля ИЛИ
2. JSON файл стиля ИЛИ
3. Скриншот конфигурации карты из приложения

И я помогу интегрировать точно такой же стиль на веб-сайт! 🗺️
