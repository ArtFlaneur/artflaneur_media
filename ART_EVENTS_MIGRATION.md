# Миграция управления арт-ивентами в Sanity CMS

## ✅ Что сделано

### 1. Создана схема `artEvent` в Sanity Studio
- **Файл**: [`apps/studio/schemaTypes/artEvent.ts`](apps/studio/schemaTypes/artEvent.ts)
- **Поля**:
  - name, slug, type, discipline
  - startDate, endDate  
  - city, country, region
  - website, instagram, email, organizer
- **Особенности**:
  - Группы полей для удобной навигации
  - Validation rules для дат и обязательных полей
  - Красивые preview с иконками
  - Сортировка по дате

### 2. Скрипт импорта данных из CSV
- **Файл**: [`apps/studio/scripts/importArtEvents.ts`](apps/studio/scripts/importArtEvents.ts)
- **Команда**: `npm run import:art-events`
- **Функции**:
  - Парсинг CSV с обработкой BOM
  - Конвертация дат M/D/YYYY → YYYY-MM-DD
  - Создание slug из названий
  - Нормализация URL
  - Пропуск невалидных записей

### 3. GROQ запросы
- **Файл**: [`apps/web/sanity/lib/queries.ts`](apps/web/sanity/lib/queries.ts)
- **Запросы**:
  - `ALL_ART_EVENTS_QUERY` - все события
  - `ART_EVENTS_QUERY` - с фильтрацией по датам
  - `ART_EVENT_QUERY` - одно событие по slug

### 4. Обновлен фронтенд
- **Файл**: [`apps/web/pages/ArtEventsCalendar.tsx`](apps/web/pages/ArtEventsCalendar.tsx)
- Теперь загружает данные из Sanity вместо CSV

## 🚀 Как использовать

### Шаг 1: Импортировать существующие данные

```bash
cd apps/studio

# Убедитесь, что у вас есть write token в .env файле:
# SANITY_STUDIO_WRITE_TOKEN=your_token_here

npm run import:art-events
```

**Примечание**: Для импорта нужен Write Token. Получите его в [Sanity Management](https://www.sanity.io/manage):
1. Перейдите в свой проект
2. API → Tokens
3. Создайте новый токен с правами Editor или Admin
4. Добавьте в `apps/studio/.env`:
   ```
   SANITY_STUDIO_WRITE_TOKEN=sk...
   ```

### Шаг 2: Управление событиями через Sanity Studio

После импорта вы можете:
- Открыть Sanity Studio (`npm run dev` в `apps/studio`)
- Найти "Art Event" в меню
- Добавлять/редактировать/удалять события через удобный интерфейс
- Использовать фильтры и поиск
- Все изменения сразу отразятся на сайте

### Шаг 3: Добавление новых событий

Теперь вы можете добавлять события двумя способами:

#### Через Sanity Studio (рекомендуется):
1. Откройте Studio
2. Нажмите "Create" → "Art Event"
3. Заполните поля
4. Нажмите "Publish"

#### Через CSV (для массового импорта):
1. Обновите [`apps/web/public/data/art-events.csv`](apps/web/public/data/art-events.csv)
2. Запустите `npm run import:art-events`

## 📝 Формат данных

### Даты
- В Sanity: `YYYY-MM-DD` (ISO формат)
- В CSV: `M/D/YYYY` (автоматически конвертируется)

### Типы событий
- Art Fair
- Biennale
- Triennale
- Annual Festival
- Art Weekend

### Регионы
- Africa
- Americas
- Asia
- Europe
- Middle East
- Oceania

## 🔧 Технические детали

### Генерация типов

После изменения схемы всегда запускайте:

```bash
cd apps/studio
npm run typegen
```

Это обновит TypeScript типы для фронтенда.

### Структура документа в Sanity

```typescript
{
  _type: 'artEvent',
  _id: 'artEvent-art-basel',
  name: 'Art Basel',
  slug: { current: 'art-basel' },
  type: 'Art Fair',
  discipline: 'Contemporary Art',
  startDate: '2026-06-18',
  endDate: '2026-06-21',
  city: 'Basel',
  country: 'Switzerland',
  region: 'Europe',
  website: 'https://www.artbasel.com',
  instagram: '@artbasel',
  email: '',
  organizer: ''
}
```

## ⚠️ Миграция завершена

CSV файл больше не используется для загрузки данных на сайте. Теперь все данные берутся из Sanity CMS.

Вы можете удалить старый CSV файл или оставить его как backup.

## 🎉 Преимущества новой системы

✅ **Удобное редактирование** - визуальный интерфейс вместо Excel  
✅ **Валидация данных** - невозможно сохранить некорректное событие  
✅ **История изменений** - Sanity хранит все версии  
✅ **Совместная работа** - несколько редакторов могут работать одновременно  
✅ **API доступ** - данные доступны через GROQ API  
✅ **Превью изменений** - можно увидеть изменения до публикации  
✅ **Поиск и фильтры** - встроенные в Studio  

## 📧 Вопросы?

Если что-то не работает или есть вопросы - пишите!
