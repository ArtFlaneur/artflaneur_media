# 🔒 Аудит безопасности API ключей

## ✅ Выполненные проверки

### 1. Защита API ключей

**Проблемы найдены и исправлены:**

- ❌ В [CLIENT_GRAPHQL_API_ACCESS.md](CLIENT_GRAPHQL_API_ACCESS.md) был реальный Firebase API ключ → **Заменен на плейсхолдер**
- ❌ В [apps/studio/GRAPHQL_SEARCH_GUIDE.md](apps/studio/GRAPHQL_SEARCH_GUIDE.md) был реальный GraphQL API ключ → **Заменен на плейсхолдер**

### 2. Обновлен .gitignore

**Корневой .gitignore** (`/.gitignore`):
```gitignore
# GraphQL configuration
*.graphql
.graphqlrc*
graphql.config.*
codegen.yml
codegen.yaml

# API Keys and Environment files
.env*
!.env.example
```

**Web приложение** (`/apps/web/.gitignore`):
```gitignore
# Environment variables
.env
.env.local
.env.*.local

# GraphQL configuration
*.graphql
.graphqlrc*
graphql.config.*
```

### 3. Созданы примеры конфигурации

- ✅ `apps/web/.env.example` - пример переменных окружения для web приложения
- ✅ `apps/studio/.env.example` - пример переменных окружения для Sanity Studio
- ✅ `SECURITY.md` - политика безопасности проекта

## 🔍 Текущее состояние

### Защищенные файлы (не в git)

Следующие файлы **корректно игнорируются** git:
- `.env`
- `apps/studio/.env`
- `apps/web/.env.local`

### Файлы в документации

Все примеры API ключей в документации заменены на плейсхолдеры:
- `YOUR_FIREBASE_API_KEY`
- `da2-your-api-key-here`
- `your_project_id`

## ⚠️ Критически важно

### Если ключи уже были закоммичены

Если реальные API ключи были закоммичены в историю git, необходимо:

1. **Немедленно отозвать/изменить все ключи:**
   - Firebase API Key
   - GraphQL API Key (AWS AppSync)
   - Supabase Keys
   - Sanity API Tokens

2. **Очистить историю git:**
```bash
# Используйте BFG Repo-Cleaner
brew install bfg
bfg --replace-text passwords.txt
git reflog expire --expire=now --all
git gc --prune=now --aggressive

# Или git filter-branch (медленнее)
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch apps/studio/.env" \
  --prune-empty --tag-name-filter cat -- --all
```

3. **Force push изменений:**
```bash
git push origin --force --all
git push origin --force --tags
```

## 📋 Чеклист для разработчиков

При работе с проектом:

- [ ] Создайте локальные `.env` файлы из примеров
- [ ] Никогда не коммитьте файлы с реальными ключами
- [ ] Проверяйте `git status` перед коммитом
- [ ] Используйте переменные окружения платформы для продакшена
- [ ] При обнаружении утечки ключа - немедленно его отзывайте

## 🛡️ Рекомендации

1. **Используйте secrets management:**
   - AWS Secrets Manager
   - GitHub Secrets (для CI/CD)
   - Vercel Environment Variables

2. **Ограничьте права доступа:**
   - API ключи должны иметь минимально необходимые права
   - Используйте разные ключи для dev/staging/production

3. **Мониторинг:**
   - Настройте алерты на необычную активность API
   - Регулярно ротируйте ключи

4. **Code review:**
   - Проверяйте PR на наличие ключей перед мержем
   - Используйте pre-commit hooks для сканирования

## 📚 Дополнительные ресурсы

- [SECURITY.md](SECURITY.md) - Политика безопасности
- [GitHub Security Best Practices](https://docs.github.com/en/code-security)
- [OWASP API Security](https://owasp.org/www-project-api-security/)
